export const metadata = {
  title: "Profile",
  description: "This is the profile page of this application",
};

const layout = ({ children }) => {
  return <div>{children}</div>;
};

export default layout;
